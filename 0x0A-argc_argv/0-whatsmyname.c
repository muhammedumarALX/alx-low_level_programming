#include "main.h"
#include <stdio.h>

/**
 * main - program that prints it name followed by new line
 * @argc: argument counter
 * @argv: argument vector
 * Return: Nothing
 */

int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
